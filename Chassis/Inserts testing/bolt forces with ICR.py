import numpy as np

# Mount type: 'A-ARM' or 'SHOCK'
mounttype = 'A-Arm'  # Change to 'SHOCK' for shock mount


inputs = np.array([6255.272,	3392.495,	0.000])

# find the instantaneous center of rotation (ICR) based on input forces
